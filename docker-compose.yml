services:
  # 1. ส่วนของ Backend (Go)
  go_backend: # 👈 ชื่อนี้ต้องตรงกับที่เขียนใน nginx.conf (proxy_pass)
    build: ./purchasing-system # (หรือ path ที่เก็บโค้ด Go)
    ports:
      - "8000:8000"
    environment:
      - DB_DSN=... # ใส่ค่า config ของ Go ตรงนี้

  # 2. ส่วนของ Frontend (React + Nginx)
  frontend:
    build: ./p2p-front-end # (path ที่เก็บโค้ด React)
    ports:
      - "80:80" # เปิดพอร์ต 80 ให้เข้าเว็บได้
    depends_on:
      - go_backend # บอกให้รอ Backend รันก่อน